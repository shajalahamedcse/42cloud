import React from 'react';
import MemChart from './MemChart';

class Mem extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <MemChart />
    )
  }
}

export default Mem;