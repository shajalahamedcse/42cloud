import React, { Component } from 'react';
import MemChart from './MemChart';

class Mem extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <MemChart />
    )
  }
}

export default Mem;